Run the KeyShield.sql file to create the database and insert the default data. Important! Make sure that the server port and the database configuration port match; by default, it is set to port 3307.
